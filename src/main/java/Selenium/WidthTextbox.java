package Selenium;

public class WidthTextbox {

    //driver.findElement(By.xpath(“xpath of textbox ”)).getSize().getWidth();
    //driver.findElement(By.xpath(“xpath of textbox ”)).getSize().getHeight();
}
